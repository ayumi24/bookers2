<h2>Book detail</h2>
<table>
  <tbody>
  <tr>
    <td><%= image_tag @user.get_profile_image(100,100) %><br>
    <%= link_to @user.name,user_path(@user.id) %>
    </td>
    <td><p><%= link_to @books.title, book_path(@books.id) %></p></td>
    <td><p><%= @books.body %></p></td>
    <% if @books.user == current_user %>
    <td><%= link_to "Edit", edit_book_path(@books.id) %></td>
    <td><%= link_to "Destroy", book_path(@books.id),method: :destroy %></td>
    <% end %>
  </tr>
  </tbody>
</table>
